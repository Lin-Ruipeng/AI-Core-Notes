# `p3` 程序说明

## 快速开始

一键运行

```bash
bash ./p3.sh
```

## 结果说明

程序会通过 `execvp()` 这个API接口运行wc程序(单词计数程序)

注意这个接口和 `fork()` 不同! 

`fork()` 只是复制自己, 而 `exec()` 则会把指定程序覆盖自己, 
所以这两者搭配使用就实现了, 先复制自己, 然后运行别人, 
就相当于运行了另外一个程序作为子进程!

其他部分的说明请查看 `p1` 和 `p2` 程序
